<h1> Chores </h1>

<%  @chores.each do |chore|  %>
  <div id="chore-<%= chore.id %>">
    <h3><%= chore.title %></h3>
    <p>Duration(in hours): <%= chore.duration %></p>
    <p>Daily chore: <%= chore.daily %></p>
  </div>
<% end %>
